// Education data is now merged into the Experience film tape section (Experiences.jsx).
// This component is kept as a no-op placeholder for import compatibility.
const Ed_Timeline = () => null;
export default Ed_Timeline;
